import {FunctionComponent, useEffect} from "react";

interface HomeProps {}

const Home: FunctionComponent<HomeProps> = () => {
	useEffect(() => {}, []);
	return (
		<>
			<h1>Home</h1>
		</>
	);
};

export default Home;
